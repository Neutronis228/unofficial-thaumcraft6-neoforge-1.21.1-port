plugins {
    id 'java-library'
    id 'maven-publish'
    id 'net.neoforged.moddev' version '2.0.141'
    id 'idea'
}

version = mod_version
group = mod_group_id

base {
    archivesName = mod_id
}

java.toolchain.languageVersion = JavaLanguageVersion.of(21)

tasks.named('wrapper', Wrapper).configure {
    distributionType = Wrapper.DistributionType.BIN
}

sourceSets.main.resources {
    srcDir 'src/generated/resources'
    exclude '**/*.bbmodel'
    exclude 'src/generated/**/.cache'
}

repositories {
}

neoForge {
    version = project.neo_version

    parchment {
        mappingsVersion = project.parchment_mappings_version
        minecraftVersion = project.parchment_minecraft_version
    }

    runs {
        client {
            client()
            systemProperty 'neoforge.enabledGameTestNamespaces', project.mod_id
        }

        server {
            server()
            programArgument '--nogui'
            def runServerWorld = providers.gradleProperty('tcRunServerWorld').orNull ?: System.getProperty('tc.runServerWorld')
            if (runServerWorld != null) {
                programArgument '--world'
                programArgument runServerWorld
            }
            def runServerPort = providers.gradleProperty('tcRunServerPort').orNull ?: System.getProperty('tc.runServerPort')
            if (runServerPort != null) {
                programArgument '--port'
                programArgument runServerPort
            }
            systemProperty 'neoforge.enabledGameTestNamespaces', project.mod_id
        }

        data {
            data()
            programArguments.addAll(
                    '--mod', project.mod_id,
                    '--all',
                    '--output', file('src/generated/resources/').getAbsolutePath(),
                    '--existing', file('src/main/resources/').getAbsolutePath()
            )
        }

        configureEach {
            systemProperty 'forge.logging.markers', 'REGISTRIES'
            def aspectDumpEnabled = providers.gradleProperty('tcAspectDump').orNull ?: System.getProperty('tc.aspectDump')
            def aspectDumpPath = providers.gradleProperty('tcAspectDumpPath').orNull ?: System.getProperty('tc.aspectDumpPath')
            def scanDumpEnabled = providers.gradleProperty('tcScanDump').orNull ?: System.getProperty('tc.scanDump')
            def scanDumpPath = providers.gradleProperty('tcScanDumpPath').orNull ?: System.getProperty('tc.scanDumpPath')
            def scanEntityDumpEnabled = providers.gradleProperty('tcScanEntityDump').orNull ?: System.getProperty('tc.scanEntityDump')
            def scanEntityDumpPath = providers.gradleProperty('tcScanEntityDumpPath').orNull ?: System.getProperty('tc.scanEntityDumpPath')
            def researchRequirementAuditEnabled = providers.gradleProperty('tcResearchRequirementAudit').orNull ?: System.getProperty('tc.researchRequirementAudit')
            def researchRequirementAuditPath = providers.gradleProperty('tcResearchRequirementAuditPath').orNull ?: System.getProperty('tc.researchRequirementAuditPath')
            def researchRequirementAuditDetailLimit = providers.gradleProperty('tcResearchRequirementAuditDetailLimit').orNull ?: System.getProperty('tc.researchRequirementAuditDetailLimit')
            def researchDataAuditEnabled = providers.gradleProperty('tcResearchDataAudit').orNull ?: System.getProperty('tc.researchDataAudit')
            def researchDataAuditPath = providers.gradleProperty('tcResearchDataAuditPath').orNull ?: System.getProperty('tc.researchDataAuditPath')
            def researchPageCatalogAuditEnabled = providers.gradleProperty('tcResearchPageCatalogAudit').orNull ?: System.getProperty('tc.researchPageCatalogAudit')
            def researchPageCatalogAuditPath = providers.gradleProperty('tcResearchPageCatalogAuditPath').orNull ?: System.getProperty('tc.researchPageCatalogAuditPath')
            def thaumonomiconProtocolAuditEnabled = providers.gradleProperty('tcThaumonomiconProtocolAudit').orNull ?: System.getProperty('tc.thaumonomiconProtocolAudit')
            def thaumonomiconProtocolAuditPath = providers.gradleProperty('tcThaumonomiconProtocolAuditPath').orNull ?: System.getProperty('tc.thaumonomiconProtocolAuditPath')
            def arcaneRecipeAuditEnabled = providers.gradleProperty('tcArcaneRecipeAudit').orNull ?: System.getProperty('tc.arcaneRecipeAudit')
            def arcaneRecipeAuditPath = providers.gradleProperty('tcArcaneRecipeAuditPath').orNull ?: System.getProperty('tc.arcaneRecipeAuditPath')
            def arcaneWorkbenchAuditEnabled = providers.gradleProperty('tcArcaneWorkbenchAudit').orNull ?: System.getProperty('tc.arcaneWorkbenchAudit')
            def arcaneWorkbenchAuditPath = providers.gradleProperty('tcArcaneWorkbenchAuditPath').orNull ?: System.getProperty('tc.arcaneworkbenchAuditPath')
            def crucibleBehaviorAuditEnabled = providers.gradleProperty('tcCrucibleBehaviorAudit').orNull ?: System.getProperty('tc.crucibleBehaviorAudit')
            def crucibleBehaviorAuditPath = providers.gradleProperty('tcCrucibleBehaviorAuditPath').orNull ?: System.getProperty('tc.crucibleBehaviorAuditPath')
            def specialAlchemyBehaviorAuditEnabled = providers.gradleProperty('tcSpecialAlchemyBehaviorAudit').orNull ?: System.getProperty('tc.specialAlchemyBehaviorAudit')
            def specialAlchemyBehaviorAuditPath = providers.gradleProperty('tcSpecialAlchemyBehaviorAuditPath').orNull ?: System.getProperty('tc.specialAlchemyBehaviorAuditPath')
            def thaumatoriumBehaviorAuditEnabled = providers.gradleProperty('tcThaumatoriumBehaviorAudit').orNull ?: System.getProperty('tc.thaumatoriumBehaviorAudit')
            def thaumatoriumBehaviorAuditPath = providers.gradleProperty('tcThaumatoriumBehaviorAuditPath').orNull ?: System.getProperty('tc.thaumatoriumBehaviorAuditPath')
            def mirrorBehaviorAuditEnabled = providers.gradleProperty('tcMirrorBehaviorAudit').orNull ?: System.getProperty('tc.mirrorBehaviorAudit')
            def mirrorBehaviorAuditPath = providers.gradleProperty('tcMirrorBehaviorAuditPath').orNull ?: System.getProperty('tc.mirrorBehaviorAuditPath')
            def lampDeviceAuditEnabled = providers.gradleProperty('tcLampDeviceAudit').orNull ?: System.getProperty('tc.lampDeviceAudit')
            def lampDeviceAuditPath = providers.gradleProperty('tcLampDeviceAuditPath').orNull ?: System.getProperty('tc.lampDeviceAuditPath')
            def infernalFurnaceBehaviorAuditEnabled = providers.gradleProperty('tcInfernalFurnaceBehaviorAudit').orNull ?: System.getProperty('tc.infernalFurnaceBehaviorAudit')
            def infernalFurnaceBehaviorAuditPath = providers.gradleProperty('tcInfernalFurnaceBehaviorAuditPath').orNull ?: System.getProperty('tc.infernalFurnaceBehaviorAuditPath')
            def alchemyAutomationDeviceAuditEnabled = providers.gradleProperty('tcAlchemyAutomationDeviceAudit').orNull ?: System.getProperty('tc.alchemyAutomationDeviceAudit')
            def alchemyAutomationDeviceAuditPath = providers.gradleProperty('tcAlchemyAutomationDeviceAuditPath').orNull ?: System.getProperty('tc.alchemyAutomationDeviceAuditPath')
            def brainJarBehaviorAuditEnabled = providers.gradleProperty('tcBrainJarBehaviorAudit').orNull ?: System.getProperty('tc.brainJarBehaviorAudit')
            def brainJarBehaviorAuditPath = providers.gradleProperty('tcBrainJarBehaviorAuditPath').orNull ?: System.getProperty('tc.brainJarBehaviorAuditPath')
            def standaloneDeviceBlockerAuditEnabled = providers.gradleProperty('tcStandaloneDeviceBlockerAudit').orNull ?: System.getProperty('tc.standaloneDeviceBlockerAudit')
            def standaloneDeviceBlockerAuditPath = providers.gradleProperty('tcStandaloneDeviceBlockerAuditPath').orNull ?: System.getProperty('tc.standaloneDeviceBlockerAuditPath')
            def entityFoundationAuditEnabled = providers.gradleProperty('tcEntityFoundationAudit').orNull ?: System.getProperty('tc.entityFoundationAudit')
            def entityFoundationAuditPath = providers.gradleProperty('tcEntityFoundationAuditPath').orNull ?: System.getProperty('tc.entityFoundationAuditPath')
            def entitySpawnPolicyAuditEnabled = providers.gradleProperty('tcEntitySpawnPolicyAudit').orNull ?: System.getProperty('tc.entitySpawnPolicyAudit')
            def entitySpawnPolicyAuditPath = providers.gradleProperty('tcEntitySpawnPolicyAuditPath').orNull ?: System.getProperty('tc.entitySpawnPolicyAuditPath')
            def infusionBehaviorAuditEnabled = providers.gradleProperty('tcInfusionBehaviorAudit').orNull ?: System.getProperty('tc.infusionBehaviorAudit')
            def infusionBehaviorAuditPath = providers.gradleProperty('tcInfusionBehaviorAuditPath').orNull ?: System.getProperty('tc.infusionBehaviorAuditPath')
            def researchTableAuditEnabled = providers.gradleProperty('tcResearchTableAudit').orNull ?: System.getProperty('tc.researchTableAudit')
            def researchTableAuditPath = providers.gradleProperty('tcResearchTableAuditPath').orNull ?: System.getProperty('tc.researchTableAuditPath')
            def essentiaTransportBehaviorAuditEnabled = providers.gradleProperty('tcEssentiaTransportBehaviorAudit').orNull ?: System.getProperty('tc.essentiaTransportBehaviorAudit')
            def essentiaTransportBehaviorAuditPath = providers.gradleProperty('tcEssentiaTransportBehaviorAuditPath').orNull ?: System.getProperty('tc.essentiaTransportBehaviorAuditPath')
            def itemEquipmentBehaviorAuditEnabled = providers.gradleProperty('tcItemEquipmentBehaviorAudit').orNull ?: System.getProperty('tc.itemEquipmentBehaviorAudit')
            def itemEquipmentBehaviorAuditPath = providers.gradleProperty('tcItemEquipmentBehaviorAuditPath').orNull ?: System.getProperty('tc.itemEquipmentBehaviorAuditPath')
            def focusCasterCoreAuditEnabled = providers.gradleProperty('tcFocusCasterCoreAudit').orNull ?: System.getProperty('tc.focusCasterCoreAudit')
            def focusCasterCoreAuditPath = providers.gradleProperty('tcFocusCasterCoreAuditPath').orNull ?: System.getProperty('tc.focusCasterCoreAuditPath')
            def focusCastExecutionAuditEnabled = providers.gradleProperty('tcFocusCastExecutionAudit').orNull ?: System.getProperty('tc.focusCastExecutionAudit')
            def focusCastExecutionAuditPath = providers.gradleProperty('tcFocusCastExecutionAuditPath').orNull ?: System.getProperty('tc.focusCastExecutionAuditPath')
            def warpBehaviorAuditEnabled = providers.gradleProperty('tcWarpBehaviorAudit').orNull ?: System.getProperty('tc.warpBehaviorAudit')
            def warpBehaviorAuditPath = providers.gradleProperty('tcWarpBehaviorAuditPath').orNull ?: System.getProperty('tc.warpBehaviorAuditPath')
            def warpEventBehaviorAuditEnabled = providers.gradleProperty('tcWarpEventBehaviorAudit').orNull ?: System.getProperty('tc.warpEventBehaviorAudit')
            def warpEventBehaviorAuditPath = providers.gradleProperty('tcWarpEventBehaviorAuditPath').orNull ?: System.getProperty('tc.warpEventBehaviorAuditPath')
            def fluxBoreThaumatoriumAuditEnabled = providers.gradleProperty('tcFluxBoreThaumatoriumAudit').orNull ?: System.getProperty('tc.fluxBoreThaumatoriumAudit')
            def fluxBoreThaumatoriumAuditPath = providers.gradleProperty('tcFluxBoreThaumatoriumAuditPath').orNull ?: System.getProperty('tc.fluxBoreThaumatoriumAuditPath')
            def fluxRiftConsequenceAuditEnabled = providers.gradleProperty('tcFluxRiftConsequenceAudit').orNull ?: System.getProperty('tc.fluxRiftConsequenceAudit')
            def fluxRiftConsequenceAuditPath = providers.gradleProperty('tcFluxRiftConsequenceAuditPath').orNull ?: System.getProperty('tc.fluxRiftConsequenceAuditPath')
            def wispBehaviorAuditEnabled = providers.gradleProperty('tcWispBehaviorAudit').orNull ?: System.getProperty('tc.wispBehaviorAudit')
            def wispBehaviorAuditPath = providers.gradleProperty('tcWispBehaviorAuditPath').orNull ?: System.getProperty('tc.wispBehaviorAuditPath')
            def fluxTaintBlockerAuditEnabled = providers.gradleProperty('tcFluxTaintBlockerAudit').orNull ?: System.getProperty('tc.fluxTaintBlockerAudit')
            def fluxTaintBlockerAuditPath = providers.gradleProperty('tcFluxTaintBlockerAuditPath').orNull ?: System.getProperty('tc.fluxTaintBlockerAuditPath')
            def taintEcologyBlockerAuditEnabled = providers.gradleProperty('tcTaintEcologyBlockerAudit').orNull ?: System.getProperty('tc.taintEcologyBlockerAudit')
            def taintEcologyBlockerAuditPath = providers.gradleProperty('tcTaintEcologyBlockerAuditPath').orNull ?: System.getProperty('tc.taintEcologyBlockerAuditPath')
            def taintMobBlockerAuditEnabled = providers.gradleProperty('tcTaintMobBlockerAudit').orNull ?: System.getProperty('tc.taintMobBlockerAudit')
            def taintMobBlockerAuditPath = providers.gradleProperty('tcTaintMobBlockerAuditPath').orNull ?: System.getProperty('tc.taintMobBlockerAuditPath')
            def fallingTaintBlockerAuditEnabled = providers.gradleProperty('tcFallingTaintBlockerAudit').orNull ?: System.getProperty('tc.fallingTaintBlockerAudit')
            def fallingTaintBlockerAuditPath = providers.gradleProperty('tcFallingTaintBlockerAuditPath').orNull ?: System.getProperty('tc.fallingTaintBlockerAuditPath')
            def minimalGameTestFixtureEnabled = providers.gradleProperty('tcMinimalGameTestFixture').orNull ?: System.getProperty('tc.minimalGameTestFixture')
            def minimalGameTestFixturePath = providers.gradleProperty('tcMinimalGameTestFixturePath').orNull ?: System.getProperty('tc.minimalGameTestFixturePath')
            if (aspectDumpEnabled != null) {
                systemProperty 'tc.aspectDump', aspectDumpEnabled
            }
            if (aspectDumpPath != null) {
                systemProperty 'tc.aspectDumpPath', aspectDumpPath
            }
            if (scanDumpEnabled != null) {
                systemProperty 'tc.scanDump', scanDumpEnabled
            }
            if (scanDumpPath != null) {
                systemProperty 'tc.scanDumpPath', scanDumpPath
            }
            if (scanEntityDumpEnabled != null) {
                systemProperty 'tc.scanEntityDump', scanEntityDumpEnabled
            }
            if (scanEntityDumpPath != null) {
                systemProperty 'tc.scanEntityDumpPath', scanEntityDumpPath
            }
            if (researchRequirementAuditEnabled != null) {
                systemProperty 'tc.researchRequirementAudit', researchRequirementAuditEnabled
            }
            if (researchRequirementAuditPath != null) {
                systemProperty 'tc.researchRequirementAuditPath', researchRequirementAuditPath
            }
            if (researchRequirementAuditDetailLimit != null) {
                systemProperty 'tc.researchRequirementAuditDetailLimit', researchRequirementAuditDetailLimit
            }
            if (researchDataAuditEnabled != null) {
                systemProperty 'tc.researchDataAudit', researchDataAuditEnabled
            }
            if (researchDataAuditPath != null) {
                systemProperty 'tc.researchDataAuditPath', researchDataAuditPath
            }
            if (researchPageCatalogAuditEnabled != null) {
                systemProperty 'tc.researchPageCatalogAudit', researchPageCatalogAuditEnabled
            }
            if (researchPageCatalogAuditPath != null) {
                systemProperty 'tc.researchPageCatalogAuditPath', researchPageCatalogAuditPath
            }
            if (thaumonomiconProtocolAuditEnabled != null) {
                systemProperty 'tc.thaumonomiconProtocolAudit', thaumonomiconProtocolAuditEnabled
            }
            if (thaumonomiconProtocolAuditPath != null) {
                systemProperty 'tc.thaumonomiconProtocolAuditPath', thaumonomiconProtocolAuditPath
            }
            if (arcaneRecipeAuditEnabled != null) {
                systemProperty 'tc.arcaneRecipeAudit', arcaneRecipeAuditEnabled
            }
            if (arcaneRecipeAuditPath != null) {
                systemProperty 'tc.arcaneRecipeAuditPath', arcaneRecipeAuditPath
            }
            if (arcaneWorkbenchAuditEnabled != null) {
                systemProperty 'tc.arcaneWorkbenchAudit', arcaneWorkbenchAuditEnabled
            }
            if (arcaneWorkbenchAuditPath != null) {
                systemProperty 'tc.arcaneWorkbenchAuditPath', arcaneWorkbenchAuditPath
            }
            if (crucibleBehaviorAuditEnabled != null) {
                systemProperty 'tc.crucibleBehaviorAudit', crucibleBehaviorAuditEnabled
            }
            if (crucibleBehaviorAuditPath != null) {
                systemProperty 'tc.crucibleBehaviorAuditPath', crucibleBehaviorAuditPath
            }
            if (specialAlchemyBehaviorAuditEnabled != null) {
                systemProperty 'tc.specialAlchemyBehaviorAudit', specialAlchemyBehaviorAuditEnabled
            }
            if (specialAlchemyBehaviorAuditPath != null) {
                systemProperty 'tc.specialAlchemyBehaviorAuditPath', specialAlchemyBehaviorAuditPath
            }
            if (thaumatoriumBehaviorAuditEnabled != null) {
                systemProperty 'tc.thaumatoriumBehaviorAudit', thaumatoriumBehaviorAuditEnabled
            }
            if (thaumatoriumBehaviorAuditPath != null) {
                systemProperty 'tc.thaumatoriumBehaviorAuditPath', thaumatoriumBehaviorAuditPath
            }
            if (mirrorBehaviorAuditEnabled != null) {
                systemProperty 'tc.mirrorBehaviorAudit', mirrorBehaviorAuditEnabled
            }
            if (mirrorBehaviorAuditPath != null) {
                systemProperty 'tc.mirrorBehaviorAuditPath', mirrorBehaviorAuditPath
            }
            if (lampDeviceAuditEnabled != null) {
                systemProperty 'tc.lampDeviceAudit', lampDeviceAuditEnabled
            }
            if (lampDeviceAuditPath != null) {
                systemProperty 'tc.lampDeviceAuditPath', lampDeviceAuditPath
            }
            if (infernalFurnaceBehaviorAuditEnabled != null) {
                systemProperty 'tc.infernalFurnaceBehaviorAudit', infernalFurnaceBehaviorAuditEnabled
            }
            if (infernalFurnaceBehaviorAuditPath != null) {
                systemProperty 'tc.infernalFurnaceBehaviorAuditPath', infernalFurnaceBehaviorAuditPath
            }
            if (alchemyAutomationDeviceAuditEnabled != null) {
                systemProperty 'tc.alchemyAutomationDeviceAudit', alchemyAutomationDeviceAuditEnabled
            }
            if (alchemyAutomationDeviceAuditPath != null) {
                systemProperty 'tc.alchemyAutomationDeviceAuditPath', alchemyAutomationDeviceAuditPath
            }
            if (brainJarBehaviorAuditEnabled != null) {
                systemProperty 'tc.brainJarBehaviorAudit', brainJarBehaviorAuditEnabled
            }
            if (brainJarBehaviorAuditPath != null) {
                systemProperty 'tc.brainJarBehaviorAuditPath', brainJarBehaviorAuditPath
            }
            if (standaloneDeviceBlockerAuditEnabled != null) {
                systemProperty 'tc.standaloneDeviceBlockerAudit', standaloneDeviceBlockerAuditEnabled
            }
            if (standaloneDeviceBlockerAuditPath != null) {
                systemProperty 'tc.standaloneDeviceBlockerAuditPath', standaloneDeviceBlockerAuditPath
            }
            if (entityFoundationAuditEnabled != null) {
                systemProperty 'tc.entityFoundationAudit', entityFoundationAuditEnabled
            }
            if (entityFoundationAuditPath != null) {
                systemProperty 'tc.entityFoundationAuditPath', entityFoundationAuditPath
            }
            if (entitySpawnPolicyAuditEnabled != null) {
                systemProperty 'tc.entitySpawnPolicyAudit', entitySpawnPolicyAuditEnabled
            }
            if (entitySpawnPolicyAuditPath != null) {
                systemProperty 'tc.entitySpawnPolicyAuditPath', entitySpawnPolicyAuditPath
            }
            if (infusionBehaviorAuditEnabled != null) {
                systemProperty 'tc.infusionBehaviorAudit', infusionBehaviorAuditEnabled
            }
            if (infusionBehaviorAuditPath != null) {
                systemProperty 'tc.infusionBehaviorAuditPath', infusionBehaviorAuditPath
            }
            if (researchTableAuditEnabled != null) {
                systemProperty 'tc.researchTableAudit', researchTableAuditEnabled
            }
            if (researchTableAuditPath != null) {
                systemProperty 'tc.researchTableAuditPath', researchTableAuditPath
            }
            if (essentiaTransportBehaviorAuditEnabled != null) {
                systemProperty 'tc.essentiaTransportBehaviorAudit', essentiaTransportBehaviorAuditEnabled
            }
            if (essentiaTransportBehaviorAuditPath != null) {
                systemProperty 'tc.essentiaTransportBehaviorAuditPath', essentiaTransportBehaviorAuditPath
            }
            if (itemEquipmentBehaviorAuditEnabled != null) {
                systemProperty 'tc.itemEquipmentBehaviorAudit', itemEquipmentBehaviorAuditEnabled
            }
            if (itemEquipmentBehaviorAuditPath != null) {
                systemProperty 'tc.itemEquipmentBehaviorAuditPath', itemEquipmentBehaviorAuditPath
            }
            if (focusCasterCoreAuditEnabled != null) {
                systemProperty 'tc.focusCasterCoreAudit', focusCasterCoreAuditEnabled
            }
            if (focusCasterCoreAuditPath != null) {
                systemProperty 'tc.focusCasterCoreAuditPath', focusCasterCoreAuditPath
            }
            if (focusCastExecutionAuditEnabled != null) {
                systemProperty 'tc.focusCastExecutionAudit', focusCastExecutionAuditEnabled
            }
            if (focusCastExecutionAuditPath != null) {
                systemProperty 'tc.focusCastExecutionAuditPath', focusCastExecutionAuditPath
            }
            if (warpBehaviorAuditEnabled != null) {
                systemProperty 'tc.warpBehaviorAudit', warpBehaviorAuditEnabled
            }
            if (warpBehaviorAuditPath != null) {
                systemProperty 'tc.warpBehaviorAuditPath', warpBehaviorAuditPath
            }
            if (warpEventBehaviorAuditEnabled != null) {
                systemProperty 'tc.warpEventBehaviorAudit', warpEventBehaviorAuditEnabled
            }
            if (warpEventBehaviorAuditPath != null) {
                systemProperty 'tc.warpEventBehaviorAuditPath', warpEventBehaviorAuditPath
            }
            if (fluxBoreThaumatoriumAuditEnabled != null) {
                systemProperty 'tc.fluxBoreThaumatoriumAudit', fluxBoreThaumatoriumAuditEnabled
            }
            if (fluxBoreThaumatoriumAuditPath != null) {
                systemProperty 'tc.fluxBoreThaumatoriumAuditPath', fluxBoreThaumatoriumAuditPath
            }
            if (fluxRiftConsequenceAuditEnabled != null) {
                systemProperty 'tc.fluxRiftConsequenceAudit', fluxRiftConsequenceAuditEnabled
            }
            if (fluxRiftConsequenceAuditPath != null) {
                systemProperty 'tc.fluxRiftConsequenceAuditPath', fluxRiftConsequenceAuditPath
            }
            if (wispBehaviorAuditEnabled != null) {
                systemProperty 'tc.wispBehaviorAudit', wispBehaviorAuditEnabled
            }
            if (wispBehaviorAuditPath != null) {
                systemProperty 'tc.wispBehaviorAuditPath', wispBehaviorAuditPath
            }
            if (fluxTaintBlockerAuditEnabled != null) {
                systemProperty 'tc.fluxTaintBlockerAudit', fluxTaintBlockerAuditEnabled
            }
            if (fluxTaintBlockerAuditPath != null) {
                systemProperty 'tc.fluxTaintBlockerAuditPath', fluxTaintBlockerAuditPath
            }
            if (taintEcologyBlockerAuditEnabled != null) {
                systemProperty 'tc.taintEcologyBlockerAudit', taintEcologyBlockerAuditEnabled
            }
            if (taintEcologyBlockerAuditPath != null) {
                systemProperty 'tc.taintEcologyBlockerAuditPath', taintEcologyBlockerAuditPath
            }
            if (taintMobBlockerAuditEnabled != null) {
                systemProperty 'tc.taintMobBlockerAudit', taintMobBlockerAuditEnabled
            }
            if (taintMobBlockerAuditPath != null) {
                systemProperty 'tc.taintMobBlockerAuditPath', taintMobBlockerAuditPath
            }
            if (fallingTaintBlockerAuditEnabled != null) {
                systemProperty 'tc.fallingTaintBlockerAudit', fallingTaintBlockerAuditEnabled
            }
            if (fallingTaintBlockerAuditPath != null) {
                systemProperty 'tc.fallingTaintBlockerAuditPath', fallingTaintBlockerAuditPath
            }
            if (minimalGameTestFixtureEnabled != null) {
                systemProperty 'tc.minimalGameTestFixture', minimalGameTestFixtureEnabled
            }
            if (minimalGameTestFixturePath != null) {
                systemProperty 'tc.minimalGameTestFixturePath', minimalGameTestFixturePath
            }
            logLevel = org.slf4j.event.Level.DEBUG
        }
    }

    mods {
        "${mod_id}" {
            sourceSet sourceSets.main
        }
    }
}

def generateModMetadata = tasks.register('generateModMetadata', ProcessResources) {
    def replaceProperties = [
            minecraft_version      : minecraft_version,
            minecraft_version_range: minecraft_version_range,
            neo_version            : neo_version,
            loader_version_range   : loader_version_range,
            mod_id                 : mod_id,
            mod_name               : mod_name,
            mod_license            : mod_license,
            mod_version            : mod_version,
            mod_authors            : mod_authors,
            mod_description        : mod_description
    ]

    inputs.properties replaceProperties
    expand replaceProperties
    from 'src/main/templates'
    into 'build/generated/sources/modMetadata'
}

sourceSets.main.resources.srcDir generateModMetadata
neoForge.ideSyncTask generateModMetadata

publishing {
    publications {
        register('mavenJava', MavenPublication) {
            from components.java
        }
    }

    repositories {
        maven {
            url "file://${project.projectDir}/repo"
        }
    }
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
}

idea {
    module {
        downloadSources = true
        downloadJavadoc = true
    }
}
